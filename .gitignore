# Build output
/build/

# Sigil dependency resolution artifacts
/.sigil/
/.sigil-cache/

# Editor / OS noise
*~
*.log
.wrangler/

# a developer's dependency redirects (sigil deps install --redirects dev-redirects.sgl), never the lock's
dev-redirects.sgl
